<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <h4>posted by <%= post.user.email %></h4>
    <h6><%= pluralize(post.views, "view") %></h6>
    <%= post.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

</div>
